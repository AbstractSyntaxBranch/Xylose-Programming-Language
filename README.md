# Xylose Programming Language
A programming language aimed at providing simplicity and ease of use.

To note: this language is still in very early development and is not usable in any extent yet.
## Building
To build you can cd into the cloned source and type `xmake build` or `xmake run` in the terminal.
To install type `xmake install`.
## Dependencies
- XMake
